#include <stdio.h>

int main()
{
	int a,b;
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("value1 is greater");
    }
    else if(a<b)
    {
        printf("value2 is greater");
    }
    else if(a==b)
    {
        printf("both values are equal");
    }
	return 0;
}
